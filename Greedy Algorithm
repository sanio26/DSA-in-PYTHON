#Greedy algorithm
amount=8
coins=[1,2,5]
coins.sort(reverse=True)
count=0
for coin in coins:
    count+=amount//coin
    amount=amount%coin
if amount==0:
    print(count)
else:
    print("cannot make change")
